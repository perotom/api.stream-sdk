# Generated by "buf config migrate-v1beta1". Edit as necessary, and
# remove this comment when you're finished.
#
# This module represents the "proto/idl" root found in
# the previous configuration file for the
# "buf.build/lightstream/proto" module.
version: v1
name: buf.build/lightstream/idl
deps:
  - buf.build/grpc-ecosystem/grpc-gateway
  - buf.build/googleapis/googleapis
breaking:
  use:
    - FILE
lint:
  use:
    - DEFAULT
  allow_comment_ignores: true